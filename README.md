## 使用cmake的安卓可执行程序构建demo

## 设置环境变量 `NDK_ROOT` 为NDK路径

## windows上编译步骤
    1. python bootstrap.py android ninja 
    2. python build.py android ninja windows

## 其他平台上将步骤二中的
windows可修改为linux,mac，产物依然为android可执行程序 
